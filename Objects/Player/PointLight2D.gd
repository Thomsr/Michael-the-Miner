extends PointLight2D

var t = 0
var up = true;

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	#if(texture_scale >= .99):
	#	up = false
	#if(texture_scale <= .951):
	#	up = true
	#	
	#if(not up):
	#	texture_scale = lerpf(texture_scale, .95, .01)
	#else:
	#	texture_scale = lerpf(texture_scale, 1, .01)	
	#
	#print(texture_scale)
	pass
